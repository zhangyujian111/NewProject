using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 自定义组件 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(666);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
